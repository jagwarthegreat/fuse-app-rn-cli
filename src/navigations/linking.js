const config = {
    screens: {
        LoginScreen: 'loginScreen',
        SignUpScreen: 'signupScreen',
    },
}

const linking = {
    prefixes: ['https://rnapp'],
    config,
}

export default linking;